import React from 'react';
import { Box, Typography, Button, Grid, useTheme } from '@mui/material';

const BottomNavi = () => {
  const theme = useTheme();
  
  return (
  <Box>BottomNavigation</Box>
  );
}

export default BottomNavi;
